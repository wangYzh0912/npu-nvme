# Copy this file to .build_config and adapt it to the target machine.
# .build_config is intentionally ignored because it contains local paths.

export SPDK_ROOT_DIR="/absolute/path/to/npu-nvme/third_party/spdk"
export SOC_VERSION="Ascend910B3"
