# 预实验结果

这里仅保留具有后续复核价值的早期原始数据。它们不是当前整理分支的正式回归结果，
也不应直接作为论文或答辩中的最终对比证据。

`step1/` 中三份 JSON 记录了 2026 年 6 月的 GPT-2 XL 单卡初步测量。原文件没有
写入源码提交号，包含旧机器路径，且部分 SPDK 分项计时与端到端墙钟时间口径不同。
因此只能用于估算量级和设计后续实验，正式图表需要在固定提交、固定配置和正确性
门禁通过后重新采集。

其余无效或来源不清的历史输出已从开发分支移除，仍可在远端
`codex/pre-cleanup-archive` 中追溯。
