# cmake/func.cmake — builds the cmake utility library
# Contains helper functions for operator compilation.
# Symlink to wait_probe's cmake infrastructure to avoid code duplication.

include(${CMAKE_CURRENT_SOURCE_DIR}/cmake/lib/cmake_func.cmake)