{
  "schema_version": 1,
  "profile_id": "EN-native",
  "scope": "Post-run verification of preserved real source-002/restore-003 hardware evidence plus worker preflight faults. This runner does not execute new training. Hardware provenance is in the original run manifests and logs.",
  "positive_hardware_runs": ["source-002", "restore-003"],
  "cases": [
    {"id": "EN-recorded-native-restart", "tier": "CPU", "nodeid": "tests/gates/test_native_restart_evidence.py", "timeout_seconds": 60, "required": true},
    {"id": "EN-preflight-and-ready-faults", "tier": "FAKE", "nodeid": "tests/python/test_qwen_native_state.py", "timeout_seconds": 60, "required": true},
    {"id": "EN-payload-auditor", "tier": "CPU", "nodeid": "tests/python/test_safetensors_audit.py", "timeout_seconds": 60, "required": true}
  ],
  "boundaries": ["fixed token rows and zero dropout", "same TP4/DP1/PP1", "no reshard", "no raw SPDK", "no E0 environment promotion or Ours claim"]
}
