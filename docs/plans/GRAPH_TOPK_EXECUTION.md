# 图内 Top-K：2026-09-17 重新实验

旧队列已停止，完整保存路径的证据单独保留。本轮不包含 NVMe、socket、JSON 协调、payload SHA、介质回读、影子 loss 或 D2H。

主模型 Qwen3-8B，辅模型 Qwen3-4B；TP4、seq128、micro-batch1、FP32 权重/BF16 计算。每配置默认一次，证据不清最多两次；正式区间连续 20 个优化器 step。三档选择比例与扫描比例是独立变量。

## 数据与依赖

G1 非空读取真实权重；G2 真实 W/R 扫描归约；G3 差分；G4 64K 全局逻辑块平方和；G5 独立 TP 通信组聚合；G6 排序 Top-K 和设备输出。q<1 按每个参数循环相位均匀选块，保留各层及参数类型。TP 列切分的部分块先在设备归约，再聚合，不能以局部 Top-K 代替。

参考由确定初始权重建立为 `W_initial * .99 + (parameter_index % 17 + 1) * 1e-5`，固定且非退化。该轮测成本，不评价保存状态误差。初始 FULL 仅在计时外用于恢复模型、Adam、步数和 RNG。

已接受的串行图按 F_t→B_t→U_t→A_t 执行，A_t 的完成成为本次返回 loss 的依赖，因此下一 step 的前向在辅助完成后提交。每个正式更新恰好检测一次，不额外排空一次评分。目标并行变体尝试在前一更新与下一更新之间执行 A，仅增加 A→U；它没有通过编译验收，不进入正式性能表。训练图的副作用规则可能自动加强依赖；“parallel”参数只表示所请求的依赖布局，只有编译图及设备轨迹确认后才报告实际并发。

所有选中索引、分数及检测版本保存在设备。参数写入必须依赖辅助结果，输出缓冲每轮仅在前一轮结束后复用。G5 使用 `graph_topk_aux_tp4`，与训练通信域分开，所有 rank 调用顺序一致。

## 计时与验收

编译运行 3 步，恢复共同 FULL，重置参考，然后各组统一非测量预热 12 步。开始前完成所有任务，正式 20 步内仅内存记录结果，结束后等待最后辅助链完成。训练回调原有的 loss 获取在各组一致；不使用每步 Host synchronize 建立新增依赖。诊断 profiler 和图转储独立于性能运行。

G0 使用原训练 wrapper，G1～G6 的训练计算沿用同一 wrapper 逻辑。先校验 G0/G1 数值一致、版本计数与非空输出，再做 K10 全扫描 G2～G6 串行与目标并行，扩 K5/K20，然后对瓶颈链测 q=1/8、1/4、1/2、1。1×通过才增加负载。G7/G8 仅对通过或接近预算配置添加，最后用 Qwen3-4B 复核代表点；不宣称跨家族泛化。

必须报告总完成时间、相对 G0/G1 增量、末尾排空、辅助链独立时间、训练段变化、额外峰值 HBM、实际重叠及测量缺口。通过条件是 3% 减速、无积压且无源数据越过写入截止点，同时列 1%/5%。单个通过点只称已测可行点。G7/G8 的设备消费者接受不等于持久化。

## 入口

```bash
python3 tools/graph_topk_run.py --level 0 --output /models/npu_nvme_exp/user7-stack/NEW_GRAPH_RUN
python3 tools/graph_topk_run.py --level 1 --layout parallel --profile --dump-graphs --steps 3 --output /models/npu_nvme_exp/user7-stack/NEW_GRAPH_DIAGNOSTIC
python3 tools/publish_graph_topk.py
```

不得覆写运行目录。硬件锁与旧路径共用；任何 retained lease 必须先诊断存活进程与设备。原始数据保留本机，远端只发布源码、配置、进度与紧凑结果。

原 4 步恢复后预热的 G0 试跑发现前段渐热，已统一增加至 12 步；旧试跑不进入新口径的减速比较。

## 编译能力调试记录

首次目标并行图在 `StepParallel` 阶段长时间递归，尚未进入训练计时，已停止并保留 IR/调用栈。串行实现改为当前更新后执行辅助链；目标并行的简化图先作为能力探针，存在额外边界检测时不进入正式性能表。只有解决严格 20 次边界及轨迹校验后才能用于容量结论。

## 当前实现能力记录

MindSpore 2.7.1 的两种目标并行依赖图均在 `StepParallel` 布局传播阶段长时间递归，未进入训练。本轮将其标记为实现能力缺口。串行 G1 设备轨迹确认：辅助读在当前更新状态写入后开始，并在下一次写入前结束；没有观测到与训练 F/B 的任务交集。后续串行 G2～G8 只回答当前算子实现的附加成本和工作量边界，不回答可并行隐藏量。

## 大 K 输出语义与诊断修复

Ascend `TopK(sorted=True)` 对 124976 输入、K=12498 的固定随机设备探针返回正确选集及阈值，但有 4159 处逆序。G6 首次完整运行因此被拒绝。接受的实现追加设备端 `Sort(descending=True)` 并重排索引，排序时间与空间计入 G6。修复后的完整 G6 四卡均通过顺序、选集阈值和真实 TP 片段 FP64 分数 oracle。

首次 G5 的计时后校验曾因各 rank 本地抽样数量不同触发 AllGather count 不一致；该尝试不进入性能表。校验现使用统一的全局索引集合，重试通过。以上均不是训练 collective 与辅助 collective 的顺序冲突。

## G7/G8 的当前设备实现

当前最小扫描点 q=1/8 的检测链相对两次 G0 减速 8.672%～15.172%，未达到“通过或接近预算”的入口条件。以下实现仅通过 CPU 几何测试，尚未在硬件运行，不能作为 G7/G8 正确性、持续运行或性能证据。本轮不扩展其正式矩阵。

选中块按全局块位置输出每卡部分值，不归属该卡的片段填零；四卡部分值之和重构原始选中块。当前实现会先重排扫描候选的本地 tile，再 gather 选中块，因此临时候选缓冲和四卡合计 4 倍 dense 输出空间必须单独报告，不能冒称紧凑物理打包。

G8 在设备端消费输出之后用 `where + assign` 推进参考，仅选中块改变值，但实现会读写完整参考参数。它的实际流量不等于 q×K 的有效更新量。每块更新次数及检测版本保存在设备；每次正式 G8 从共同预热后的确定状态重新建立参考。参考、输出和训练权重都在串行图中受完成依赖保护。它不代表介质提交。

## 基线波动

两次 20 步 G0 分别为 12.869301 s 和 12.143034 s，spread 为 5.807%。遵守最多两次策略，不临时追加第三次以挑选基线。报告保留相对两次基线的减速范围；该范围不是置信区间。3% 通过结论保持未定，但相对两次基线都超出预算的配置可明确排除。
